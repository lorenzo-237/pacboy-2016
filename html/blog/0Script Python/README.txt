# --- UTILISATION --- # 

copiercollerstructure.py

Après que la page soit crée, l'ouvrir avec un éditeur de texte comme
comme Sublime Text ou Notepad+ et choisir l'encodage UTF-8.
Ensuite vérifier les accents dans le code.
(Le script ne gère pas bien l'encodage)